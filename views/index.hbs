
  <div class="m-2 p-2 ">
    <form action="/short" method="POST">
      <input class="form-control" type="url" name="short" placeholder="input url here" value="{{baseurl}}" style="width: 60vw" >
      <button type="submit" class="btn btn-success m-2 p-2">Click me to Shorten</button>
    </form>
    {{#if baseurl}}
      <div class="text-start">
        {{#if originUrl}}
        <div class="alert alert-danger mr-2 fs-4 text-center" role="alert">You have been create this Link</div>
        {{else}}
        <div class="alert alert-success mr-2 fs-4 text-center" role="alert">▼ Success! Please use this Link ▼</div>
        {{/if}}
        <div class=" fs-3 d-inline-block align-middle" id="copy" style="width: 50vw">{{reurl}}</div>
        <button class="btn btn-info d-inline-block align-middle ml-2 btn-copyUrl">Copy Url</button>
      </div>
    {{/if}}
    {{#if notFoundUrl}}
      <div class="alert alert-danger mr-2 fs-4 text-center" role="alert">▲ Oops! Please input URL ▲</div>
    {{/if}}
  </div>
</div>

<script src="/javaScripts/copyurl.js"></script>